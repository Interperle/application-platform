version: '3.8'

services:
  application-platform:
    image: ghcr.io/generation-d/application-platform-dev:latest
    ports:
      - "3000:3000"  # Map the port from your container to host if needed
    environment:
      - NEXT_PUBLIC_SUPABASE_URL=""
      - NEXT_PUBLIC_SUPABASE_ANON_KEY=""
      - SUPABASE_SERVICE_ROLE_KEY=""
      - NEXT_PUBLIC_LOGFLARE_API_TOKEN=""
      - NEXT_PUBLIC_LOGFLARE_CLIENT_TOKEN=""
      - SMTP_HOST=""
      - SMTP_PORT=""
      - SMTP_USER=""
      - SMTP_PASSWORD=""
